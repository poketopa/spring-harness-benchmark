# Concurrent Login Prompt Sufficiency Case (EN L5)

## Case Metadata

- Feature: `concurrent-login`
- Prompt language: `en`
- Prompt level: `L5`
- Oracle: `benchmarks/prompt-sufficiency-cases/roomescape/oracles/concurrent-login.md`
- Baseline path: `benchmarks/roomescape-jpa-auth-manager-authorization-regen-v2`
- Repeats: `001`, `002`, `003`
- Status: `planned`

## Purpose

This case tests whether `spring-usecase-implementation` can produce or safely refuse backend work when the prompt is at `L5` quality.

The prompt quality changes by level, but the hidden oracle remains strict. A low-quality prompt is allowed to be incomplete; the evaluator still checks backend behavior, architecture, tests, and verification against the oracle.

## Implementation Prompt Payload

Use only this section as the implementation prompt for prompt-sufficiency baseline runs.

Implement the concurrent login policy. When the same member logs in again, only the newest login remains valid and older tokens must no longer work. Other members' logins must not be affected. Use server-side session state or an equivalent storage design to validate active sessions, and return intentional authentication failure for old tokens. Same-member concurrent login races must not leak database constraint exceptions as 500 responses. Add tests for new token success, old token failure, other member token unaffected, and a real concurrent login test, then run full verification.

## Evaluation Notes

- Do not inspect sibling prompt levels before completing a run.
- Do not update the skill/reference during the 84-run baseline set.
- Record assumptions, ambiguity, missing verification, semantic drift, architecture drift, and test gaps explicitly.
- Evaluate against the hidden oracle even when the prompt does not mention every oracle criterion.
