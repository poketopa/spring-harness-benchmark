# Prompt-Sufficiency Blind Runs

This directory is reserved for prompt-only blind pilot target projects.

Do not place shared solution source here. Each child directory must be created from the baseline path listed in `benchmarks/benchmark-records/prompt-sufficiency-blind-matrix.csv`.

Implementation agents should work inside a single target project and should not inspect sibling blind targets.
